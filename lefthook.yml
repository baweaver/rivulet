pre-commit:
  commands:
    rspec:
      glob: "**/*.rb"
      run: bundle exec rspec
    rubyfmt:
      glob: "**/*.rb"
      run: rubyfmt --check {staged_files}

pre-push:
  commands:
    rspec:
      run: bundle exec rspec
